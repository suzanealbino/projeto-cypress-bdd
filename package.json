{
  "name": "projeto-cypress-bdd",
  "version": "1.0.0",
  "scripts": {
    "test": "cypress run --spec 'cypress/e2e/**/*.feature'"
  },
  "devDependencies": {
    "@badeball/cypress-cucumber-preprocessor": "20.0.0",
    "@bahmutov/cypress-esbuild-preprocessor": "2.2.0",
    "cypress": "13.6.0",
    "esbuild": "0.19.0"
  }
}
